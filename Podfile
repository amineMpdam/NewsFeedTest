# Uncomment the next line to define a global platform for your project
platform :ios, '26.1'

target 'NewsFeedTest' do
  use_frameworks!
  # Pods for NewsFeedTest
  pod 'Sourcery', :subspecs => ['CLI-Only']

  target 'NewsFeedTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
